name: Publish

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.release.tag_name }}
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org/
      - run: npm install -g npm@^11.15.0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Build
        run: bun run build
      - name: Type check
        run: bun run type-check
      - name: Lint
        run: bun run lint
      - name: Run tests
        run: bun run test
      - name: Verify the publishable tarball
        run: npm pack --dry-run
      - name: Verify release tag matches package version
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: node -e "const v = require('./package.json').version; const tag = process.env.RELEASE_TAG; if (tag !== 'v' + v) throw new Error('Release tag ' + tag + ' does not match package version v' + v);"
      - name: Publish to npm
        run: npm publish
